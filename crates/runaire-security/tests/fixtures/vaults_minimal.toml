# US-051 fallback fixture: a registered vault with NO `[vault.lock]`
# section. Consumed by `tests/us_051_idle_auto_lock.rs::per_vault_override_absent_falls_back_to_default`
# (Phase 4 T4.2 un-ignores that case). `VaultLockConfig::from_extra`
# should return `None` for this vault, and the controller should be
# built with `AutoLockConfig::default()` (600s).

version = 1

[[vault]]
name = "personal"
path = "/tmp/personal-default.kdbx"
created_at = "2026-05-21T10:00:00-06:00"
